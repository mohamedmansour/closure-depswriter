Google Closure depswriter in Java. 

To run:
java -jar depswriter.jar --library=/path/to/closure/goog --source=/path/to/source

To compile, jars it in output folder:
ant jar

Eclispe Compatible.
